# pythonFlashWeb

简单的 Flask 样板应用，包含主页 `/` 和 `/hello`。

## 运行

```bash
python -m venv .venv
.venv\\Scripts\\activate
pip install -r requirements.txt
python app.py
```

访问:
- http://127.0.0.1:5000/
- http://127.0.0.1:5000/hello
